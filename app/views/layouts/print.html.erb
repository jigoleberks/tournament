<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Results" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind" %>
  </head>
  <body class="bg-white text-slate-900">
    <main class="max-w-3xl mx-auto p-6">
      <%= yield %>
    </main>
  </body>
</html>
